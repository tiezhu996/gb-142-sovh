<!-- 改编自：人文关怀主题项目提示词/gb-142/关怀通知推送服务.md；后端由 未指定技术栈 改造为 Go 1.22 + Gin + GORM -->

请根据以下要求生成项目代码和 README。

## 项目类型：纯后端服务

## 项目概述
构建一个关怀通知推送服务，维护关怀对象名单，按设定的关怀频率定时发送问候短信；家属可订阅通知，关怀对象超期未回复确认时自动向家属发送告警；管理员可查询所有对象状态；短信模板库按节日、天气、季节分类管理；记录每次短信发送日志并提供外部查询接口。

## 功能需求

1. **关怀对象管理**：维护关怀对象名单，字段包括姓名、手机号、关怀频率（如每天/每周/每月）、关怀开始时间、状态（Active/Paused）、创建时间。支持新增、编辑、删除、查询关怀对象。

2. **定时问候短信**：系统按关怀对象的关怀频率定时自动发送问候短信；短信内容从模板库中随机选择。定时调度使用 Go 的 `robfig/cron/v3` 实现。

3. **家属订阅与告警**：家属可订阅指定关怀对象的通知；关怀对象超期未回复确认（超过可配置超时时间）时，系统自动向家属发送告警短信。

4. **回复确认**：提供接口接收关怀对象的回复确认（短信回调或手动标记），更新关怀对象最近确认时间和状态。

5. **管理员状态查询**：管理员可查询所有关怀对象状态，区分“平安”（近期已确认）和“失联”（超期未确认）。

6. **模板库管理**：短信模板按节日、天气、季节分类，支持增删改查。发短信时按分类或随机选择模板。

7. **发送日志与外部查询接口**：记录每条短信的发送时间、接收人、模板内容、发送结果（成功/失败/失败原因），提供查询接口供外部系统使用。

## 技术要求

- **英文短名（用于 Docker Compose）**：`gbcarenotify`
- **后端端口**：3242（Docker 内部统一使用 8080）
- **数据库端口**：5742
- **后端**：Go 1.22 + Gin + GORM
- **数据库**：MySQL 8.0
- **定时调度**：`github.com/robfig/cron/v3`
- **短信发送**：定义 `SMSProvider` 接口，默认提供 `LogSMSSender`（记录日志并写库），生产环境可替换为真实短信服务商实现
- **日志**：Go 标准库 `log/slog`，通过中间件输出结构化日志
- **认证**：不引入完整用户体系；外部查询接口使用 `API_KEY` 进行简单鉴权（从环境变量读取）

## 后端目录结构（Go 标准布局）

后端代码放在 `backend/`，禁止将 handler/service/repository/model 合并到单一文件：

```
backend/
├── go.mod
├── go.sum
├── cmd/
│   └── server/
│       └── main.go            # 程序入口：加载配置、初始化 DB、启动 cron 调度、注册路由、启动 HTTP 服务
└── internal/
    ├── config/                # 配置结构体与加载逻辑（端口、数据库、API_KEY、超时时间等）
    ├── model/                 # GORM 模型：CareRecipient、FamilySubscription、SMSTemplate、SMSLog、ReplyConfirm 等
    ├── repository/            # 数据访问层：RecipientRepository、TemplateRepository、SMSLogRepository、SubscriptionRepository 等
    ├── service/               # 业务逻辑层：RecipientService、NotificationService、TemplateService、AlertService、StatusService、SMSService 等
    ├── scheduler/             # 定时任务：使用 robfig/cron/v3 注册问候短信和超时告警任务
    ├── handler/               # Gin HTTP 处理层：RecipientHandler、TemplateHandler、SMSLogHandler、AlertHandler、AdminHandler 等
    ├── router/                # 路由注册：集中定义路由分组和中间件挂载
    ├── middleware/            # 中间件：APIKeyMiddleware、CORS、请求日志、统一错误处理等
    ├── dto/                   # 请求/响应结构体与参数校验标签
    └── constants/             # 错误码、关怀频率、对象状态、短信发送结果等枚举
```

数据库初始化脚本放在 `database/init.sql`。
## Go 企业级工程规范（必须遵循）

后端 Go 代码除满足上述功能与分层要求外，还必须遵循当前 Go 语言通用的企业级工程规范：

### 1. 项目目录规范
- 根目录 `go.mod` 声明 Go 版本 `go 1.22`，模块名使用公司/项目规范（如 `github.com/<org>/<project>`）。
- 采用 `cmd/` + `internal/` + `pkg/` 布局：`cmd/server/main.go` 只负责加载配置、装配依赖、启动服务；私有业务代码全部放在 `internal/`；仅无业务依赖的可复用库放 `pkg/`。
- 数据库迁移脚本放 `migrations/`，接口定义/OpenAPI 文档放 `api/`，Docker/K8s 部署文件放 `deploy/`。
- 包名使用小写单词、不加下划线；文件使用 `snake_case.go`；导出标识符用 `CamelCase`，未导出的用 `camelCase`；禁止使用拼音命名。

### 2. 分层与依赖方向
- 依赖方向严格单向：`handler → service → repository → model`，`model` 不依赖任何上层，禁止反向引用与包循环依赖。
- 每个核心实体按 `model_<entity>.go`、`repository_<entity>.go`、`service_<entity>.go`、`handler_<entity>.go` 分文件，职责单一。
- 依赖通过构造函数注入：`func NewXService(repo Repo, logger *slog.Logger) *XService`；禁止在业务代码中全局 `init` 连接数据库或散落 `sql.Open`。

### 3. 错误处理
- 使用 `fmt.Errorf("list users: %w", err)` 包裹并透传错误，保留错误链。
- 仓储层定义哨兵错误（如 `var ErrNotFound = errors.New("not found")`），上层用 `errors.Is` 判断。
- 禁止在 service 层打印日志后吞掉错误；错误统一由 `middleware` 或 `handler` 转换为标准 JSON 响应。
- 所有 error 必须被处理，关键错误禁止 `_ =` 忽略。

### 4. 配置与日志
- 配置通过环境变量注入，在 `internal/config/config.go` 集中解析，推荐使用 `github.com/caarlos0/env/v11` 或 `spf13/viper`。
- 日志使用 `log/slog` 或 `go.uber.org/zap` 结构化输出；请求日志统一包含 `request_id`、`method`、`path`、`status`、`latency_ms`。

### 5. 参数校验与响应
- 入参校验使用 `github.com/go-playground/validator/v10`，在 DTO 结构体 tag 中声明规则。
- 路由统一 `/api/v1` 前缀；健康检查为 `/healthz`；分页参数统一为 `page`、`page_size`。
- 响应统一为 `{ "code": 0, "message": "ok", "data": ... }` 包裹，错误码集中维护。

### 6. 测试与文档
- 单元测试使用标准库 `testing` + 表驱动（table-driven），文件命名 `*_test.go`；service 与 repository 必须有对应测试。
- 对外接口提供 OpenAPI/Swagger（如 `github.com/swaggo/gin-swagger`）或在 README 中列出完整 API 清单。


## 部署要求

项目必须支持 Docker Compose 一键部署。

需要提供以下文件：
- 根目录 `docker-compose.yml`（编排 backend + MySQL 8.0）
- 根目录 `.env` 和 `.env.example`：包含 `COMPOSE_PROJECT_NAME=gbcarenotify`、`DB_NAME`、`DB_USER`、`DB_PASSWORD`、`DB_ROOT_PASSWORD`、`DB_PORT`、`BACKEND_PORT`、`API_KEY`、`CONFIRM_TIMEOUT_MINUTES`
- `backend/Dockerfile`：Go 多阶段构建
- `database/init.sql`：数据库初始化脚本

backend/Dockerfile 必须使用 Go 多阶段构建：
```dockerfile
FROM golang:1.22-alpine AS builder
WORKDIR /app
COPY go.mod go.sum ./
RUN go mod download
COPY . .
RUN CGO_ENABLED=0 GOOS=linux go build -o /app/server ./cmd/server

FROM alpine:3.20
RUN apk add --no-cache ca-certificates tzdata
WORKDIR /app
COPY --from=builder /app/server .
EXPOSE 8080
CMD ["/app/server"]
```

docker-compose.yml 要求：
- **不要写 `version:` 字段**
- 顶层声明 `name: gbcarenotify`
- 容器名带 `gbcarenotify-` 前缀
- backend 服务内部端口统一为 `8080`，对外映射 `${BACKEND_PORT:-3242}:8080`
- MySQL 对外映射 `${DB_PORT:-5742}:3306`
- 数据库数据使用命名卷持久化
- MySQL 配置 `healthcheck`，backend `depends_on` 等待数据库就绪
- 支持 `docker compose up -d` 一条命令启动

## 构建验证清单（硬性约束）

- 后端必须能通过 `go mod tidy` 生成完整 `go.mod`/`go.sum`，依赖声明包括 `github.com/gin-gonic/gin`、`gorm.io/gorm`、`gorm.io/driver/mysql`、`github.com/robfig/cron/v3` 等
- 后端必须能通过 `go build ./...`；本地运行入口为 `go run ./cmd/server`
- 禁止将 handler、service、repository、model、middleware、dto、constants 职责合并到单一文件
- 不得使用 `version:` 字段，Compose V2 顶层声明 `name: gbcarenotify`

## README 写法

根目录必须有 `README.md`，用中文说明项目。

README 包含：项目名称和一句话介绍、项目主要功能、快速启动方式（Docker Compose）、本地开发方式（`go mod tidy`、`go run ./cmd/server`）、技术栈（表格展示，后端写 Go 1.22 + Gin + GORM）、项目目录结构（代码块展示）、主要 API 列表、环境变量说明、License。

要求：Docker 部署命令放在最前面；告知用户首次启动前执行 `cp .env.example .env`；说明后端端口 3242、数据库端口 5742。

## Git 要求

代码编写完成后：
1. 在项目根目录初始化 Git 仓库（`git init`）
2. 创建 `.gitignore` 文件，至少包含：
   ```
   .env
   .DS_Store
   *.log
   bin/
   ```
3. 执行 `git add -A && git commit -m "init: 关怀通知推送服务初始化"`
